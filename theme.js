// Shared theme configuration for Tailwind CSS
const themeConfig = {
    theme: {
        extend: {
            colors: {
                primary: '#a80000',
                secondary: '#6e0000',
            }
        }
    }
};
